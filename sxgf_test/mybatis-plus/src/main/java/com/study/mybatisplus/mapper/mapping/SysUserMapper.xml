<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.mybatisplus.mapper.SysUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="SysUser">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="pass_word" property="passWord" />
        <result column="real_name" property="realName" />
        <result column="user_remark" property="userRemark" />
        <result column="user_salt" property="userSalt" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, pass_word, real_name, user_remark, user_salt, create_time
    </sql>

    <select id="login" resultType="SysUser">
        select <include refid="Base_Column_List"/> from sys_user
        where  user_name = #{userName} and pass_word = #{passWord}
    </select>

    <select id="selectAll" resultType="SysUser">
        select <include refid="Base_Column_List"/> from sys_user
        <where>
            <if test="userName!=null and userName!=''">
                user_name = #{userName}
            </if>
            <if test="passWord!=null and passWord!=''">
                and pass_word = #{passWord}
            </if>
        </where>
    </select>
</mapper>
